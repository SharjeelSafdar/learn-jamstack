import React from "react";

import Layout from "../components/Layout";
import styles from "./desc.module.css";

export default () => {
  return (
    <Layout>
      <h2 className={styles.myComponent}>How with component styles</h2>
    </Layout>
  );
};
